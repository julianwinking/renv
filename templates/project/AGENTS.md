# {{title}} — agent notes

This project is driven by the parent `reref` research environment. Follow the
parent `AGENTS.md` protocol: restore context (`reref exp list {{slug}}`,
`reref log list {{slug}}`) before working; cite every literature claim; generate
every number from a recorded run; log decisions before pivoting; one experiment,
one question.

Use `@reref:<kind>:<id>` comment tags in code to link a fix/implementation back to
its originating paper, finding, or decision (e.g. `# @reref:finding:42:fixes ...`).
