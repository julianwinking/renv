# {{title}} — project agent instructions

This project **inherits** the parent environment's protocol (`../../AGENTS.md`)
and the writing guides (`../../templates/writing/`). Those are the single
source of truth — nothing from them is repeated here, so nothing can drift.

## Project-specific overrides

(none yet — add only what is genuinely different in THIS project: domain
vocabulary, extra invariants, dataset quirks. Protocol changes belong in the
parent `AGENTS.md`, editable in the cockpit under Admin → Instructions.)

## Code↔store links

Use `@reref:<kind>:<id>[:<rel>]` comment tags to link code to its originating
finding/decision/paper (e.g. `# @reref:finding:42:fixes`).
